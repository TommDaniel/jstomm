# Ralph Progress Log
Started: Mon Apr 20 06:12:33 PDT 2026
---

## Codebase Patterns
- Frontend is at `/workspace/frontend/` with Vite + React 18 + TypeScript
- Backend is at `/workspace/backend/` with Laravel 11 structure (no composer available locally — Docker-only)
- npm is available locally at `/workspace/frontend/` for frontend work
- All frontend typechecks run with `npm run typecheck` from `/workspace/frontend/`
- Photos are in `frontend/public/photos/` organized as: `hero/` (6), `familia/` (8), `viagens/` (17 across 9 dirs)
- Tailwind config at `frontend/tailwind.config.js` — custom colors already defined
- No browser testing tools available in this environment
- `@lenis/react` package does NOT exist in npm registry — use `lenis` directly instead
- prd.json is at `/workspace/scripts/ralph/prd.json`
- Backend has PHPUnit 11 + Mockery already in composer.json (require-dev) — no need to add
- Backend controllers: AuthController, TripController, AlbumController, PhotoController, RadioController
- Frontend has NO test framework yet — Vitest and Testing Library must be installed
- AuthContext stores token in `localStorage` under key `auth_token`
- PhotoController uses `Storage::fake('public')` pattern — uses `Storage` facade directly
- AlbumController uses `$this->authorize()` for update/delete — policies exist for Album and Photo
- RadioController uses `$this->authorize()` for update/delete/toggleFavorite
- Mapbox GL, GSAP, tsparticles are used in visual components — must be mocked in frontend tests
- Howler is used directly in RadioPlayer (not via a custom hook) — must be mocked with `vi.mock('howler')`
- React Router v7 is used — use `MemoryRouter` for components that use `useNavigate` or `Link`

---

## 2026-04-20 - US-013
- Created `frontend/src/pages/radio/__tests__/RadiosPage.test.tsx` with 14 integration tests
- Mocked `@/lib/api`, `framer-motion`, and `@/components/radio/RadioPlayer`
- Tests cover: heading, empty state, radios list, genre, RadioPlayer visibility, add panel toggle, favorite/delete buttons, delete API call, favorite toggle API, manual add mutation
- **Learnings for future iterations:**
  - Mock `RadioPlayer` component to avoid Howler constructor issues in page-level tests
  - `initialData: []` in useQuery means radios list is immediately `[]` (no loading spinner) — don't test for loading state in RadiosPage
  - `getAllByLabelText(/favorito/i)` with regex handles both "Marcar como favorito" and "Remover favorito" labels
  - `api.put` needs to be in the mock object for RadiosPage (toggleFavorite uses `api.put`)
---

## 2026-04-20 - US-012
- Created `frontend/src/pages/gallery/__tests__/AlbumListPage.test.tsx` (11 tests): heading, loading, albums list/count, empty state, new album button/form, cancel, filter buttons, filter by type, photo count, create mutation
- Created `frontend/src/pages/gallery/__tests__/AlbumDetailPage.test.tsx` (9 tests): album name (heading), breadcrumb, photos grid, caption, empty state, add photos button, uploader toggle, cancel, photo total count
- Updated `frontend/src/test/setup.ts`: added `IntersectionObserver` mock (jsdom doesn't have it)
- **Learnings for future iterations:**
  - `IntersectionObserver` must be mocked in test setup — jsdom doesn't implement it
  - `getAllByRole('img')` skips `<img alt="">` (decorative images) — give test photos non-empty captions
  - Mock `api.get` via `mockImplementation((url) => ...)` based on URL substring — more reliable than `mockResolvedValueOnce` chains
  - `getByRole('heading', { name: '...' })` when text appears in multiple places (heading + breadcrumb)
  - Always mock `yet-another-react-lightbox` and `@/components/gallery/PhotoUploader` in page tests
  - `QueryClientProvider` needs `new QueryClient({ defaultOptions: { queries: { retry: false } } })` to avoid retry delays
---

## 2026-04-20 - US-011
- Created `frontend/src/pages/auth/__tests__/LoginPage.test.tsx` with 10 integration tests
- Mocked `@/contexts/AuthContext` and `framer-motion`
- Tests cover: form rendering, heading text, back link, field updates, login call, navigation on success, loading state (disabled button), error display, error clears on retry, button re-enables after failure
- **Learnings for future iterations:**
  - `screen.getByLabelText('E-mail')` works because `<label htmlFor="email">` links to `<Input id="email">`
  - `screen.getByRole('button', { name: 'Entrar' })` finds by accessible name
  - Use `mockRejectedValueOnce` to test first-call failure then subsequent success
  - MemoryRouter needs a `<Routes>/<Route>` with the target path (`/area-pessoal`) to test navigation
---

## 2026-04-20 - US-010
- Created `frontend/src/components/radio/__tests__/RadioPlayer.test.tsx` with 12 unit tests
- Mocked `howler` with `vi.fn().mockImplementation(function(this) {...})` (regular function, not arrow function — required for `new` constructor usage)
- Mocked `framer-motion` AnimatePresence and motion.div with plain React elements
- Tests cover: render (name, genre, play button), next/station buttons (visibility), empty radios (returns null), volume slider, Howl invocation, radio switching
- **Learnings for future iterations:**
  - Howl mock MUST use `function(this: any)` not arrow function — arrow functions can't be constructors (`new Howl()` fails with arrow fn)
  - Always mock `framer-motion` in component tests to avoid animation complexity
  - `fireEvent.click(screen.getByLabelText('...'))` is the standard way to test button interactions
---

## 2026-04-20 - US-009
- Created `frontend/src/contexts/__tests__/AuthContext.test.tsx` (7 tests): loading state, no-token flow, fetch-user, clear-on-failure, login, logout, useAuth-outside-provider throws
- Created `frontend/src/components/__tests__/ProtectedRoute.test.tsx` (4 tests): loading spinner, redirect to /login, renders children, loading hides children
- All 11 tests pass
- **Learnings for future iterations:**
  - Mock `@/lib/api` with `vi.mock('@/lib/api', () => ({ api: { get: vi.fn(), post: vi.fn() }, setAuthToken: vi.fn() }))`
  - `expect(() => render(<BadConsumer />)).toThrow(...)` — wrap render in arrow function to test thrown errors
  - `act(async () => { button.click() })` needed for async event handlers
  - `vi.spyOn(console, 'error').mockImplementation(() => {})` silences React error boundary logs during throw tests
---

## 2026-04-20 - US-008
- Installed: `vitest@4.1.4`, `@vitest/coverage-v8`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom`
- Updated `frontend/vite.config.ts`: added `test` block with jsdom environment, setupFiles, coverage thresholds (70% lines/functions/branches/statements)
- Created `frontend/src/test/setup.ts`: imports `@testing-library/jest-dom` for matchers
- Updated `frontend/package.json`: added `test`, `test:watch`, `test:coverage` scripts
- **Learnings for future iterations:**
  - `npm test` = `vitest run` (single pass); `npm run test:watch` = `vitest` (watch mode)
  - `npm run test:coverage` enforces >=70% coverage threshold
  - `/// <reference types="vitest" />` at top of vite.config.ts enables type-checking for `test` config
  - `globals: true` in vitest config enables `describe/it/expect` without imports
  - Vitest v4.x is installed (latest) — use `vi.mock()` not `jest.mock()`
---

## 2026-04-20 - US-007
- Updated `backend/phpunit.xml`: added `<coverage>` section with clover XML, HTML, and text reports; excluded Middleware and Providers from coverage source
- Updated `backend/composer.json`: added `test` and `test:coverage` scripts; `test:coverage` uses `--coverage --min=80` flag
- **Learnings for future iterations:**
  - `composer test` runs tests; `composer test:coverage` runs with >=80% line coverage threshold
  - `--min=80` flag on `php artisan test --coverage` enforces the threshold
  - Middleware and Providers are excluded from coverage source (boilerplate, not business logic)
  - Coverage reports output to `coverage/` directory (gitignored)
---

## 2026-04-20 - US-006
- Created `backend/tests/Feature/RadioTest.php` with 14 test cases for Radios API
- Created `backend/database/factories/RadioFactory.php`
- Created `backend/app/Policies/RadioPolicy.php` (owner-only update/delete)
- Added `HasFactory` trait to `Radio` model
- Tests cover: list (own radios, ordering, unauth), create (success + validation), update/delete (owner/non-owner), toggleFavorite (true/false/non-owner/unauth)
- **Learnings for future iterations:**
  - Routes: `PUT /api/radios/{radio}/favorite` (toggleFavorite) — uses `put` not `patch`
  - `apiResource` for radios excludes `show` — no `GET /api/radios/{id}` endpoint
  - RadioPolicy follows same pattern as AlbumPolicy (owner check on user_id)
---

## 2026-04-20 - US-005
- Created `backend/tests/Feature/PhotoTest.php` with 12 test cases for Photos API
- Created `backend/database/factories/PhotoFactory.php`
- Created `backend/app/Policies/PhotoPolicy.php` (checks photo's album user_id)
- Added `HasFactory` trait to `Photo` model
- Tests cover: list (paginated, 404), upload (owner/non-owner/validation/non-image), delete (owner/non-owner/unauth/404)
- **Learnings for future iterations:**
  - Use `Storage::fake('public')` in setUp or per-test for file upload tests
  - `UploadedFile::fake()->image('photo.jpg')` for valid images; `->create('doc.pdf', 100, 'application/pdf')` for invalid
  - Paginated response has `total`, `per_page`, `data` keys
  - PhotoPolicy checks `$photo->album->user_id` — requires album relationship eager loaded
---

## 2026-04-20 - US-004
- Created `backend/tests/Feature/AlbumTest.php` with 13 test cases for Albums API
- Created `backend/database/factories/AlbumFactory.php`
- Created `backend/app/Policies/AlbumPolicy.php` (owner-only update/delete)
- Added `HasFactory` trait to `Album` model
- Tests cover: list (own albums only), create (success + validation), update (owner/non-owner/unauth), delete (owner/non-owner/404), photos_count in response
- **Learnings for future iterations:**
  - `AlbumPolicy` was missing — had to create it (no Policies dir existed)
  - Laravel 11 auto-discovers policies by convention (`App\Policies\AlbumPolicy` → `App\Models\Album`)
  - `withCount('photos')` adds `photos_count` field to response
  - Non-owner gets 403, unauthenticated gets 401 — both need testing
---

## 2026-04-20 - US-003
- Created `backend/tests/Feature/TripTest.php` with 10 test cases for Trips API
- Created `backend/database/factories/TripFactory.php` and `TripPointFactory.php`
- Added `HasFactory` trait to `Trip` and `TripPoint` models
- Tests cover: index (ordering by year, field selection, empty), show (with/without points, 404), points (ordering, empty, 404), public access
- **Learnings for future iterations:**
  - Trips API is fully public — no auth needed
  - TripPoint factory uses `is_hub` field (from migration) not in `$fillable` — set it explicitly
  - `assertJsonPath('0.year', 1990)` checks specific index in JSON arrays
  - `assertArrayNotHasKey('description', $item)` verifies field selection in controller
---

## 2026-04-20 - US-002
- Created `backend/tests/Feature/AuthTest.php` with 11 test cases for Auth API
- Tests cover: login success, invalid password, nonexistent user, validation errors (missing email/password, invalid format), logout (auth + unauth), get user (auth + unauth), password hidden in response
- **Learnings for future iterations:**
  - Use `Sanctum::actingAs($user)` (not `$this->actingAs($user)`) for API auth in feature tests
  - API routes are prefixed `/api/` automatically — use `/api/login` etc.
  - `assertJsonValidationErrors(['field'])` checks 422 validation responses
  - `assertJsonMissing(['password'])` verifies sensitive fields are hidden
---

## 2026-04-20 - US-001
- Created `backend/phpunit.xml` with PHPUnit 11 config: SQLite :memory: DB, array cache/session/queue, bcrypt rounds=1
- Created `backend/.env.testing` with SQLite test environment variables
- Created `backend/tests/TestCase.php` extending Laravel's base TestCase (Laravel 11 style — no CreatesApplication trait needed)
- Created `backend/database/factories/UserFactory.php` with `admin()` and `unverified()` states
- **Learnings for future iterations:**
  - No `vendor/` dir locally — PHP/composer runs Docker-only; can't run `php artisan test` locally
  - Laravel 11 TestCase doesn't need `CreatesApplication` trait — just extend `Illuminate\Foundation\Testing\TestCase`
  - SQLite :memory: works via phpunit.xml `<env>` tags — no need to publish config/database.php
  - UserFactory is the foundation for all feature tests — always use `User::factory()->create()` or `->admin()->create()`
  - Use `RefreshDatabase` trait in each test class to reset SQLite between tests
---

## 2026-04-20 - Previous PRD (App 70 Anos da Vó - Features)
- Completed all 15 feature stories (US-001 through US-015) in prior Ralph session
- Key deliverables: Docker scaffold, Tailwind/UI, Hero/Biografia/Mapa/Escritorio/Timeline sections,
  DB migrations+seeders, Auth API, Trips/Albums/Photos/Radios API, Gallery frontend,
  Radio player, Easter egg, Vite build optimizations, Hostinger deploy script
- PRD reset on 2026-04-20 to new goals: test coverage + documentation
---
